import { Meta, StoryObj } from '@storybook/react'
import { RulesView } from './rules'

const meta: Meta<typeof RulesView> = {
  title: 'Components/RulesView',
  component: RulesView,
}

export default meta

export const Default: StoryObj<typeof RulesView> = {}
